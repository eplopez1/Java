package class2;

public class Variables {
    public static void main(String[] args) {
        /*
        Below line is reserving a box in the computer memory.
        we are calling it box 1 if need the information that
        wqe are coding inside this box we can simply save to the computer give us the context of obox 1
        int=> what ype of data we want to store
        box1=> is the name of the box
        = sign will take whatever we write after it and will store it inside the box1
        10=> is what we are storing inside the box
         */
        int box1=10;
        // if we are printing something froma box we dont need the ""
        System.out.println(box1); // bring the contents of box1 and print them on the console

        int mybox=10000;
        System.out.println(mybox);



    }
}
